const routerUrlBase = "/vue-router-wcp";

const routerPaths = {
  MOVIE_SELECTION: `${routerUrlBase}/movie-selection`,
  FOOD_SELECTION: `${routerUrlBase}/food-selection`,
}

export { routerUrlBase, routerPaths }
